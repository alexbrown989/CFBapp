feat(n02c): scaffold explosive-vs-sustained + post-explosive momentum notebook

Adds research/notebooks/02c_explosive_vs_sustained.ipynb -- the unexecuted
notebook source for V5 trigger_features block 3 (explosive vs sustained
drives, lines 181-187) plus a momentum hypothesis derived from
research/future_features.md ("Momentum / decaying-shock features (target:
02c)" scheduled item).

Candidate set (8 features; 24 CSV rows after execution):

V5 DDL block 3 (6):
  - dog_points_from_explosives, dog_points_from_sustained,
    dog_points_from_returns -- drive-level attribution per D2/D3/D4/D12
  - dog_explosive_play_count -- D5; type-specific thresholds from 02b
  - dog_avg_drive_yards, dog_avg_drive_plays -- D6; mean over completed
    dog-offense drives

Momentum (2):
  - seconds_since_last_dog_explosive_play -- D8 primary; continuous,
    R16-safe paired with seconds_since_last_dog_explosive_play_is_null,
    per-train-window median imputation (no leakage; train-only)
  - prior_drive_had_dog_explosive_play -- D9 secondary; binary; NULL
    on drive-1 triggers per the established drive-1 null pattern

Carried verbatim from 02a / 02b: walk-forward windows (B), pre-game
baseline (alpha; 6 cols incl R16-safe spread_movement + indicator),
L1 logreg + StandardScaler + isotonic-on-val + Brier/ECE-on-test
pipeline (R7/R8), per-feature null policy (B), explosive thresholds
(D1: EXPLOSIVE_PASS_YARDS=20, EXPLOSIVE_RUSH_YARDS=12).

02c-specific decision log (from plan-approval):

  D2/D3/D4 -- drive-level points attribution. A dog-offense drive
    containing >=1 dog explosive play sends all its points to the
    explosives bucket; drives with zero dog explosives go to sustained;
    return-style TDs (KO/punt/INT/fumble/blocked-punt/blocked-FG returns)
    plus defensive 2pt + safeties go to returns. Phase 02c-c enumerates
    the cache's scoring playTypes against SCORING_PLAYTYPE_REGISTRY and
    halts loud on any unknown (D4 augmentation: same defensive-
    enumeration pattern as N01's scoring registry).
  D7 -- dog_points_from_* return 0 (not NULL) once at least one
    dog-offense drive has completed; NULL only when zero completed
    dog drives at trigger time.
  D8 -- continuous momentum: per-train-window median imputation
    computed from train only, applied to train/val/test; per-window
    median stored in a NEW imputation_value REAL column on
    feature_validation.csv. Existing 02a/02b rows receive NaN for
    this column (pd.concat unions; safe). The new column is
    documented in the 02c schema-sidecar splice.
  D10 -- pair-candidate treatment: the continuous momentum feature's
    candidate model adds both the continuous column and the _is_null
    indicator; single stability verdict keyed on the continuous name.
  D11 -- categorical-window variants (had_explosive_in_last_N_seconds
    for N in {120, 300, 600}) considered and rejected pre-execution;
    Phase 02c-i applies the P3 trigger logic at execution-report time
    (routes to future_features.md if D8 FAILs while D9 PASSes; to
    tech_debt.md if both pass or if D8 alone passes; routes momentum
    hypothesis to validated_filters.json rejected_features if both
    fail). No file edits in this commit.
  D12 -- PAT/2pt attribution: PAT (1) and successful 2pt (2) points
    attribute to the SAME bucket as the preceding TD. Explosive TD +
    successful PAT = 7 points to dog_points_from_explosives (not a
    6/1 split). Worked examples + per-trigger accounting-delta
    diagnostics in the schema sidecar.

Literature framing (P2): the 02c schema-sidecar momentum section
will reference Roebber, Burlingame, deWinter (2022) "On the existence
of momentum in professional football" (PLOS ONE 17(6):e0269604) as
the supporting evidence for the drives-level (possession-level) form
-- Roebber et al. demonstrate that NFL WP streaks across >=2
successive changes of possession are non-random. The continuous-
seconds form is a novel finding with no direct published support;
the sidecar flags it for additional scrutiny in N03 if it passes
stability here (rather than treating "PASS on continuous AND FAIL
on binary" as equally strong evidence).

Plan-time pre-execution redundancy audit: zero structural duplicates
among 02c's 8 candidates (REDUNDANT_WITH = {}). Three conditional
identities documented against 02b's drive-1 features:
  1. dog_explosive_play_count vs opening_drive_was_explosive_td
  2. dog_avg_drive_yards vs opening_drive_yards
  3. dog_avg_drive_plays vs opening_drive_plays
All conditional (hold only on subsets of triggers), not structural;
no redundant_with tags needed.

CFBD budget: scaffolds at 0 fresh CFBD calls. Phase 02c-b's cache-hit
assertion enforces zero misses at execution. Lifetime audited count
remains 253/3000 going into 02c (per research/data/cache/
cfbd_call_log.csv as of HEAD e1710a2).

Notebook generated by _build_02c.py + validated by _verify_02c.py
(both untracked scratch per the research/notebooks/_*.py convention).
Executed copy 02c_explosive_vs_sustained.executed.ipynb is
gitignored. Cells: 11 markdown + 15 code = 26 total; all code cells
ast-parse-clean; all cell IDs unique. File size 106,652 bytes.
